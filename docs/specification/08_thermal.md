# 第八章 熱計算

対象 commit・版・確認日は[第一章](01_general.md)に同じ。文書版：本文初稿 0.2。\
[全体目次](README.md)

## 1. 適用範囲・記号

ノード温度 T [°C] を未知数とし、熱流 Q [W] の収支を解く。G は W/K、C_T は J/K、Δt は s、面積 A は m²とする。RC の枝係数は面積込み、応答係数は面積当たりとする。

## 2. 熱収支及び容量

一般の一ノード収支を、流入熱を正として式 (8-1) で表す。具体的な枝の符号と設備制約を組み込んで離散化する。

$$ C_{T,i}\frac{dT_i}{dt}
=\sum_jG_{ij}(T_j-T_i)+Q_{{\rm adv},i}+Q_{{\rm gen},i}. \tag{8-1} $$

熱容量の後退差分項は C_T(T_i^{n+1}−T_i^n)/Δt とする。ビルダーは旧温度を保持する容量ノードと G_C=C_T/Δt の枝に変換する。空気と残余容量の分割は第四章による。

固定境界の温度項は右辺へ移し、未知温度の項を行列へ入れ、A_T T=b_T を解く。空調が ON の場合は制御対象の設定温度行及び関連する熱収支行を用いるため、全行を通常の自由温度収支として扱わない。

## 3. 伝導・対流及び放射

conductance 枝の出力熱流は式 (8-2) による。

$$ Q_{s\to t}=G(T_s-T_t). \tag{8-2} $$

厚さ d、熱伝導率 λ の層の抵抗は d/(λA)、容量は体積比熱 c_v に対し A d c_v である。層間・表面への配分は surface_rc の生成規則による。対流係数 h を使用する面は hA の伝熱係数へ変換する。

放射・日射・夜間放射は、ビルダーが生成した係数又は発生量を用いる。ソルバに surface の面積や日射を再度掛けて二重計上しない。面間放射の係数生成、日射の室内配分の全分岐は生成側の独立節として追加詳細化する。

## 4. 換気による熱輸送

乾き空気の実装で、枝の診断熱流は符号付き q に対して式 (8-3) とする。|q|<FLOW_RATE_MIN なら0とする。

$$ Q_{\rm adv}=\rho_0c_{pa}q(T_s-T_t). \tag{8-3} $$

この診断値は温度差による熱取得を表す。絶対エンタルピー輸送量 ρq c_p T_upstream そのものではない。ノード方程式では流向に応じた上流温度と流入側の取扱いを適用する。

moist_enthalpy_enabled の場合は式 (8-4) に置き換える。

$$ h(T,x)=(c_{pa}+c_{pv}x)T+L_vx,\quad
Q_{\rm adv}=\rho_0q\{h(T_s,x_s)-h(T_t,x_t)\}. \tag{8-4} $$

温度求解中の x は既知として扱う。空気容量の蓄積は ρ₀V{h^{n+1}−h^n}/Δt を基にする。air_capacity の係数は体積から再評価し、家具等の乾き容量は別に保持する。必要な連成条件は第六章による。

## 5. 応答係数・CTF

source 側の熱流密度は式 (8-5) の形式で扱う。

$$ q_s''(n)=\sum_{j=0}^{m}a_{s,j}T_s(n-j)
+\sum_{j=0}^{m}b_{s,j}T_t(n-j)
+\sum_{r=0}^{m-1}c_{s,r}q_s''(n-1-r). \tag{8-5} $$

target 側にも固有の係数を用いる。a、b の単位は W/(m² K)、c は無次元、回路網への熱流は A q'' [W] とする。蓄熱があるので両側熱流を常に互いの負値とみなさない。

初期温度で温度履歴を埋め、熱流履歴は0から開始する。履歴は受理された時間段階で更新し、連成反復で時間を進めない。

自動係数生成で sum_c>0.9999 の場合、U=1/Σ(d/λ) の定常係数 a=[U]、b=[−U]、c=[] へ戻す。等号はこの分岐に含まない。抵抗が正でない場合は U=0 とする。この処理では蓄熱と時間遅れを失う。

## 6. 数値処理・出力・制約

直接法による行列求解と、熱収支・線形残差の確認を行う。疎構造・数値分解を再利用するが、有効枝、風量、空調状態、必要な湿度等が変われば係数再評価の対象となる。

thermal_temperature [°C] と thermal_heat_rate_* [W] を出力する。伝導、対流、放射、capacity、advection 等は同じ熱流系列群でもモデル上の意味が異なる。面積係数、熱流の向き、旧時刻容量を確認して集計する。

## 7. 照合と根拠

G=10 W/K、T_s=20°C、T_t=5°C の伝導枝は150 W。容量のみなら (C/Δt+G)T^{n+1}=C T^n/Δt+G T_b+Q の一室既知解と比較する。

根拠：[熱流関数](../../engine/solver/core/thermal/heat_calculation.h)、[熱行列](../../engine/solver/core/thermal/thermal_direct_build.cpp)、[湿り空気](../../engine/solver/core/thermal/thermal_moist_air.h)、[応答係数生成](../../engine/app/builder/surface_response.py)、[RC生成](../../engine/app/builder/surface_rc.py)。
SPEC-08-001：容量の離散化。SPEC-08-002：CTF 面積とフォールバック。
[既存既知解テスト](../../engine/solver/tests_cpp/test_core_physics_known_solutions.cpp)。今回 C++ 実行は未実施。
2026-09-17：熱収支・移流・CTF を初稿化。
